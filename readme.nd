# Probability noising

This is a utility function to test the impact of training a classifier on one hot encoded data, and but predicting on probabilistic data. In my situation, the one-hot data is coming from labels, but the probabilistic data is the output of upstream classifiers trained on those labels.
